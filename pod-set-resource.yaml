apiVersion: v1
kind: Pod
metadata:
  name: hello
  namespace: robert
  labels:
    app: whoami
    kind: backend
spec:
  containers:
    - name: hello
      resources:
        limits:
          cpu: "1"
        requests:
          cpu: "0.5"
      image: pingbo/whoami
